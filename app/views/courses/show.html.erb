<h1> <%= @course.name %> </h1>
<h3> <%= @course.city %>, <%= @course.state %> </h3>
<h4>Hole Count: <%= @course.hole_count %></h4>
<div class="row">
  <img src="<%= @course.image_path %>" alt="Course Image">
</div>

<p> <%= link_to "Start Scorecard", new_course_scorecard_path(@course.id) %> </p>
